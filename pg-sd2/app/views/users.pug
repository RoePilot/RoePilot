extends layout

block content
  h1 Users
  if error
    p.error #{error}
  else if users && users.length
    ul
      each user in users
        li #{user.id} - #{user.name}
  else
    p No users available.
